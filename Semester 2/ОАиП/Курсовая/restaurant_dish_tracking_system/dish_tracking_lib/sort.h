#pragma once
#include "types.h"

void swapDishesInFile(int index1, int index2, const char* filename = FILENAME);
void bubbleSortByCost(const char* filename = FILENAME);
void selectionSortByCalories(const char* filename = FILENAME);
void insertionSortByName(const char* filename = FILENAME);
