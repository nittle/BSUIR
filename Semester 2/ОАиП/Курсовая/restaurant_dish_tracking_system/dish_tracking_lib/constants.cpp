#include "constants.h"

// These are the constants the user can modify centrally:
// (MAX_DISH_NAME_SIZE is in constants.h due to C++ compile-time struct
// requirements)

const char FILENAME[] = "dish_tracking_database";
const char TEMP_FILENAME[] = "temp_database";